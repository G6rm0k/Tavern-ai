# The .xcodeproj is generated by `xcodegen generate` from project.yml on every
# CI run — never commit it, there is nothing to merge-conflict on and it would
# just drift from the source of truth.
*.xcodeproj/
*.xcworkspace/
DerivedData/
build/
.build/
*.ipa
Payload/
.DS_Store
